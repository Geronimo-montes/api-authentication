export enum ERol {
  /**
   * Usuario que tiene el poder de modificar los registros
   */
  ADMIN = 'admin',
  /**
   * Solo puede utilizar las rutas de autenticación
   */
  USER = 'user',
};
